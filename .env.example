# Copy to .env and fill in. .env is gitignored.
# The web page reads this on load and prefills the key field
# (only works when served locally, e.g. python3 -m http.server).
# The Mac app reads these as environment variables when launched from a terminal.
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
